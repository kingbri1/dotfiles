layout {
    // Circumvents the default_tab_template + hide_floating_panes bug
    new_tab_template {
        pane

        pane size=1 borderless=true {
            plugin location="zjstatus"
        }
    }

    tab name="editor" focus=true hide_floating_panes=true {
        pane split_direction="vertical" {
            pane command="pwsh" size="70%" focus=true borderless=true {
                args "-NoExit" "-Command" "nvim $env:ZELLIJ_EDITOR_TARGET"
            }

            pane command="pwsh" size="30%" borderless=true {
                args "-NoExit" "-Command" "opencode --port 4096"
            }
        }

        floating_panes {
            pane {
                x "15%"
                y "15%"
                width "70%"
                height "70%"
            }
        }

        pane size=1 borderless=true {
            plugin location="zjstatus"
        }
    }
}